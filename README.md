An implementation of part of `git`: basically the product of the [Write yourself
a Git!](https://wyag.thb.lt) tutorial, but written in Rust.

Use `cargo build`/`cargo run` to build or run the project.
